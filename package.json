{
  "name": "aetna_code_test",
  "version": "1.0.0",
  "description": "## Pre-requisites",
  "main": "app.js",
  "scripts": {
    "start": "NODE_ENV=development nodemon app.js",
    "test": "NODE_ENV=test mocha --timeout 10000 --exit",
    "reinstall": "rm -rf node_modules && npm install",
    "debug": "nodemon --inspect-brk app.js",
    "debug:test": "NODE_ENV=test mocha --inspect-brk --timeout 10000 --exit"
  },
  "author": "Drew Palazzari",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.9.1",
    "request-promise": "^4.2.4",
    "sqlite3": "^4.1.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "mocha": "^6.2.0",
    "nodemon": "^1.19.2"
  }
}
